<%= render 'layouts/public_header' %>
<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render "layouts/genre_search_form" %>
    </div>
    <div class="col-md-9 content_top mb-3">
      <div class="back_image"></div>
        <p class="site_explanation">
          ようこそ、'nagano_cake'へ！</br>
          このサイトは、ケーキ販売のECサイトになります。</br>
          会員でない方も商品の閲覧はできますが、</br>
          購入には会員登録が必要になります。
        </p>
    </div>
    <div class="col-md-3"></div>
    <div class="col-md-9 mb-3">
      <h2 class="new_item">新着商品</h2>
    </div>
    <div class="col-md-3"></div>
      <% @items.each do |item| %>
        <div class="col-sm-2">
        <%= link_to public_item_path(item.id) do %>
        <table class="item_button">
          <tr>
            <td><%= image_tag item.image.url, size: "180x125" %></td>
          </tr>
          <tr>
            <td><%= item.name %></td>
          </tr>
          <tr>
            <td>¥<%= item.price %></td>
          </tr>
        </table>
        <% end %>
        </div>
      <% end %>
    <div class="col-md-12 mt-3 text-right">
      <p><%= link_to "全ての商品を見る >",public_items_path %></p>
    </div>
  </div>
</div>