<table class="genre_search_form">
  <thead class="table-active search_form_head">
    <th>ジャンル検索</th>
  </thead>
  <tbody class="genre_type">
    <% @genres.each do |genre| %>
    <tr>
      <td><%= genre.name %></td>
    </tr>
    <% end %>
  </tbody>
</table>